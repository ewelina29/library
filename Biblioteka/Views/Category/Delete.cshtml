@model Biblioteka.Models.Category

@{
    ViewBag.Title = "Delete";
}


<div class="header-title">Are you sure you want to delete this category?</div>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn default-button" /> |
            @Html.ActionLink("Back to List", "Index", null, new { @class = "redirect-button" })
        </div>
    }
</div>
